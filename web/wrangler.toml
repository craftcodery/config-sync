# Cloudflare Worker configuration
# Deploy: cd web && wrangler deploy

name = "northbuilt-setup"
main = "worker.js"
compatibility_date = "2024-01-01"

# Custom domain (configure in Cloudflare dashboard)
# routes = [
#   { pattern = "setup.northbuilt.com", custom_domain = true }
# ]
